import React, { useCallback, useState, useEffect } from "react";
const Button = (props) => {
    return (
        <button className="bg-gradient-to-r from-gray-100 to-blue-500">
            登録
        </button>
    );
};
export default Button;
